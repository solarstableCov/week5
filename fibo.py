#!/usr/bin/env python3
num=int(input("Enter a number: )
def fib(num):
    fib(0)=1
    fib(1)=1
    sequence[i]=fib(i)
    for i in range(2,num):
        #fib3=fib1+fib2
        sequence[i]=sequence[i-1]+sequence[i-2]
fib()  
print (fib(num))
